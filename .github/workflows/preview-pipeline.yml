name: PR Preview - Deployment
on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: The image tag
        required: true
        type: string
  pull_request:
    branches:
      - dev
    paths: 
      - .github/workflows/preview-pipeline.yml
      - src/**
      - Dockerfile*

permissions:
  id-token: write # This is required for requesting the JWT (OIDC)
  contents: read
  pull-requests: write

jobs:
  conduct_tag:
    runs-on: ubuntu-22.04
    steps:
      - id: checkout_latest
        name: Checkout latest
        uses: actions/checkout@v4

      - id: create_tag
        if: inputs.image_tag == ''
        name: Extract PR Info
        run: |
          short_hash=$(git rev-parse --short HEAD)
          echo "tag=${short_hash}_${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT

    outputs:
      image_tag: ${{ inputs.image_tag || steps.create_tag.outputs.tag }}

  ci:
    # Skip ci for cd testing
    if: needs.conduct_tag.outputs.image_tag == ''
    uses: ./.github/workflows/build-and-push.yml
    needs: conduct_tag
    with:
      environment: Development
      image_tag: ${{ needs.conduct_tag.outputs.image_tag }}
    secrets:
      ECR_REGION: ${{ secrets.CENTRAL_ECR_REGION }}
      OIDC_ROLE_ARN : arn:aws:iam::${{ secrets.AWS_DEV_ACCOUNT }}:role/oidc-${{ vars.REPO_ORG_ABBR }}-${{ vars.REPO_BACKEND }}
      ECR_REGISTRY: ${{ secrets.AWS_PROD_ACCOUNT }}
      ECR_REPO_BASE: ${{ secrets.CENTRAL_ECR_REPO_BASE }}
      ECR_REPO_PUBLISH: ${{ secrets.CENTRAL_ECR_REPO_PUBLISH }}
  
  cd:
    if: needs.conduct_tag.outputs.image_tag != ''
    uses: ./.github/workflows/deploy.yml
    needs: [conduct_tag, ci]
    with:
      environment: Development
      # fix image for cd testing
      # image_tag: ${{ needs.conduct_tag.outputs.image_tag }}
      image_tag: 3de32ab_72
      task_definition: dev/backend-task-def-template.json
      ecs_cluster: inff-backend
      ecs_service: dev-api-srv
      task_count: 1
    secrets:
      ECR_REGION: ${{ secrets.CENTRAL_ECR_REGION }}
      ECS_REGION: ${{ secrets.DEV_REGION }}
      OIDC_ROLE_ARN: arn:aws:iam::${{ secrets.AWS_DEV_ACCOUNT }}:role/oidc-${{ vars.REPO_ORG_ABBR }}-${{ vars.REPO_BACKEND }}
      ECR_REGISTRY: ${{ secrets.AWS_PROD_ACCOUNT }}
      ECR_REPO_BASE: ${{ secrets.CENTRAL_ECR_REPO_BASE }}
      ECR_REPO_PUBLISH: ${{ secrets.CENTRAL_ECR_REPO_PUBLISH }}
      